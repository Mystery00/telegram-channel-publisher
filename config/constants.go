package config

const (
	EnvConfigHome = "CONFIG_HOME"
	EnvLogHome    = "LOG_HOME"
)

const (
	BotToken    = "bot.token"
	ApiEndpoint = "bot.endpoint"
)

const (
	LogLocal = "log.local"
	LogHome  = "log.home"
	LogFile  = "log.file"
	LogColor = "log.color"
)

const (
	PublisherType = "publisher.type"
)
